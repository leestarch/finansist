export function handleAdminAccess(user, to, next) {
    /*if (to.meta.isAdminRoute) {
        if (user.isAdmin) {
            next();
        } else {
            next('/');
    } else {
        next();
    }*/
}
